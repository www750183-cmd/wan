// 分发配置 —— 打包发给用户前，改这一个文件就够了。
//
// 这里控制「用户第一次打开插件时看到什么」，包括：
//   · 默认的服务地址（用户不填 Base URL 时用它）
//   · 一个可选的邀请／获取 Key 的链接（留空则不显示，不会替任何服务商打广告）

export const DIST = {
  /** 默认 API 服务地址。用户不填服务地址时用它。 */
  defaultApiBase: 'https://www.manbouapi.com/v1',

  /** 默认模型名。用户不填模型名时用它。 */
  defaultModel: 'deepseek-v4.1-flash-0910',

  /**
   * 你的邀请链接。填上之后，大模型设置面板里会出现一行「还没有 Key？点这里获取」。
   * 留空 = 完全不显示，界面里不会出现任何服务商的名字。
   */
  inviteUrl: '',

  /** 邀请链接旁边的文案 */
  inviteText: '还没有 API Key？点这里获取',

  /** 邀请链接下面的小字说明（可留空） */
  inviteNote: '',
};
