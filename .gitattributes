# 统一换行符：文本入库用 LF
* text=auto eol=lf

# Windows 批处理必须保留 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 数据文件不做行尾转换，避免无意义 diff
*.json -text
*.md text eol=lf

# 二进制
*.png binary
*.jpg binary
*.ico binary
*.zip binary